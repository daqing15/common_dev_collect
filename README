*************************** 常用代码收集 ************************************
*************************** 创建者：David Chen ******************************

1.创建本仓库主要是收集一下大家在开发中使用频繁的代码，当然要原创的，而不是从
某些开源框架或软件上摘录的（如果是在其上，修改了代码，应该加上说明），

2.上传的代码要有完整的说明情况，尤其是顶端的import信息不能删除，要是引用自己的
jar包，也应该上传。

3. 上传时要加上一个代码说明文件，格式是 txt结尾的

举例子：

step 1: 在你的本地git仓库创建一个存放要上传代码的文件夹，如 xxoo（一般用你的大名，永留史册，-_-）
step 2：你的代码文件就拷贝到上一步创建的 xxoo 文件夹内，内容是：

	[注意：文件名要突出其代码的大概功能，可使用中文说明]
	package x.y.z;

	import x.y.z.XXOO 
	
	import org.apache.lang.StringXXX

	class OOXX {
	  // your code
	  // 你的代码
	  ......
	}

step 3：写上良好的代码使用说明环境，别让你的代码编程死代码
如果引用了自己编写的Jar，应该在【step 1】中创建的文件夹xxoo中
再创建一个命名为 lib 的文件夹，存放你引用的JAR包（包括源码），
如果是已有开源的框架的JAR引用就不用了,而是在【step 1】创建的文件夹下，创建文件：README（注意
没有后缀名的）

例如：
	xxoo/lib/yourxxoo.jar 
	引用apache 开源jar包
	  
README的内容是：
	
	JAR WEBSITE URL : 引用Jar的主页
	
	JAR INFORMATION : Jar简单说明
	
	JAR USE_IN_YOUR_CODE: 把你引用的类的类路径完整拷贝到这个
	
	
    COLLECTOR HERE : 可以加上

**************** 希望能得到大家的无私贡献 ************************************

*************************** 常用代码收集 ************************************